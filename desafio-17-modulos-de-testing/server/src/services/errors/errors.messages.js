export const noFindCartErrorInfo = (cid) => {
    return `El carrito con el id ${cid} no se encuentra en la base de datos`;
}

export const noFindProductErrorInfo = (pid) => {
    return `El producto con el id ${pid} no se encuentra en la base de datos`;
}

