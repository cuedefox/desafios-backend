paths:
  /api/carts/:cid:
    delete:
      summary: clean all the cart.
      tags:
        - Carrito
      responses:
        "200":
          description: Cart.
          content:
            application/json:
              schema:
                type: array