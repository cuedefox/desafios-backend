paths:
  /api/carts:
    post:
      summary: Create a cart with random id
      tags:
        - Carrito
      responses:
        "200":
          description: Cart.
          content:
            application/json:
              schema:
                type: array