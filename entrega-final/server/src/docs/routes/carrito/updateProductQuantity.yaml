paths:
  /api/carts/:cid/product/:pid:
    put:
      summary: Update product quantity by id
      tags:
        - Carrito
      responses:
        "200":
          description: Cart.
          content:
            application/json:
              schema:
                type: array