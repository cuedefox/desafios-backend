paths:
  /api/carts/:cid/product/:pid:
    delete:
      summary: Delete product from cart from id
      tags:
        - Carrito
      responses:
        "200":
          description: Cart.
          content:
            application/json:
              schema:
                type: array