paths:
  /api/carts/:cid/product/:pid:
    post:
      summary: Add product to cart by id
      tags:
        - Carrito
      responses:
        "200":
          description: Cart.
          content:
            application/json:
              schema:
                type: array